
<div class=container>
  <div class="registration-form">
<%= render "devise/shared/error_messages", resource: resource %>

    <div class="container">
      <div class="row center sign-up-form">
        <div class="col s12 l12">
          <div class="card-panel">
            <h3 class="header"><%= t('devise.login') %></h3>
              <div class="row">
                <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "col s12"}) do |f| %>
                 
                  <div class="row">
                    <div class="input-field col s12">
                      <%= f.email_field :email, placeholder: t('devise.placeholder.enter_your_email'), autofocus: true, autocomplete: "email" %>
                      <%= f.label :email %>
                    </div>
                  </div>

                  <div class="row">
                    <div class="input-field col s12">
                      <%= f.password_field :password, placeholder: t('devise.placeholder.enter_your_password'), autocomplete: "current-password" %>
                      <%= f.label :password %>
                    </div>
                  </div>

                  <div class="row">
                    <div class="input-field col s12">
                      <div class="actions">
                        <%= f.button t('devise.button.log_in'), class: "waves-effect waves-light btn-small" %>
                      </div>
                    </div>
                  </div>

                <% end %>
                
                <%= render "devise/shared/links" %>

              </div>
          </div>
        </div>
      </div>
    </div>


  </div>
</div>
